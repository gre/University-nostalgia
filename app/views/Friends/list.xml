#{extends 'base.xml' /}

#{if requests?.size()}
<requests>
#{list items: requests, as:'friend'}
  <request id="${friend.id}">
    #{userInfo user:friend /}
  </request>
#{/list}
</requests>
#{/if}

#{if friends?.size()}
<friends>
#{list items: friends, as:'friend'}
  <friend>
    #{userInfo user:friend /}
  </friend>
#{/list}
</friends>
#{/if}
